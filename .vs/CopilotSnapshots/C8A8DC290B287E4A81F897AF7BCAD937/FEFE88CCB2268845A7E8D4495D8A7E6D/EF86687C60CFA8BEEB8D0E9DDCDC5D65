using System;
using System.Windows.Forms;

namespace Otob_sBilet.R.S.UserControls
{
    public partial class BiletYazdirControl : UserControl
    {
        private IRezervasyon _rezervasyon;

        public BiletYazdirControl()
        {
            InitializeComponent();
        }

        public BiletYazdirControl(IRezervasyon rezervasyon)
        {
            InitializeComponent();
            _rezervasyon = rezervasyon;
            // Bilet bilgilerini göster ve kaydet/yazdır işlemlerini başlat
        }

        private void btnPdfKaydet_Click(object sender, EventArgs e)
        {
            MessageBox.Show("PDF olarak kaydedildi (örnek).", "Bilgi", MessageBoxButtons.OK, MessageBoxIcon.Information);
        }

        private void btnYazdir_Click(object sender, EventArgs e)
        {
            MessageBox.Show("Yazdırma işlemi başlatıldı (örnek).", "Bilgi", MessageBoxButtons.OK, MessageBoxIcon.Information);
        }
    }
}
